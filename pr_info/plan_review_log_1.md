# review-plan review log 1

## Round 1 — 2026-09-04
**Findings**:
I'll gather context: knowledge base, the issue tree, and the plan files.`pr_info/steps/step_2.md:21` — high — Step 2 removes the only `execute_command` use from `server.py`, so the import becomes dead and must be deleted; that breaks the 10 `patch("mcp_tools_py.server.execute_command")` sites outside `TestIsToolAvailable` (`tests/test_tool_availability.py:34,51,80,96,378,403,428,453,487,527`), which the step does not list.
`pr_info/steps/step_1.md:150` — high — The prescribed patch-target move (`mcp_tools_py.utils.python_environment.os.path.exists`) contradicts the step's own ALGORITHM at `step_1.md:60,66`, which uses `Path.exists()`; patching `os.path.exists` will not affect it, so the rewritten availability tests will not behave as described.
`pr_info/steps/step_1.md:156` — high — "`TestResolvePythonExecutable` may keep asserting on `server._resolved_python`" is wrong: `tests/test_tool_availability.py:43-44,60-61` compare against `os.path.join("/my/venv","Scripts","python.exe")`, while `str(Path(venv_path)/sub/exe)` yields a different string form on Windows.
`pr_info/steps/step_1.md:16` — high — `tests/test_server_params.py:83` asserts `check_code_with_pytest(..., venv_path=None, ...)`; the `venv_path`→`bin_dir` rename plus the now non-`None` value breaks it, but the file is not in step 1's Modified list.
`pr_info/steps/step_6.md:22` — high — Step 6 renames `_check_tool_availability` and deletes `_is_tool_available`, but `tests/test_server_params.py` patches/assigns them at `:52,58,104,108,141,145,411,415,500,527,746,797,799`; the file is absent from step 6's Modified list, so the step cannot land green.
`pr_info/steps/step_6.md:95` — medium — Deleting `_resolved_python` also breaks `TestResolvePythonExecutable` (`tests/test_tool_availability.py:44,61,90,103`) and `tests/test_server_params.py:78`; step 6 names only `TestToolHandlerShortCircuit` in that file.
`pr_info/steps/step_2.md:185` — medium — Acceptance criterion "`probe.py` is present in a built wheel, not only in a source checkout" is claimed closed by asserting `probe_script_path()` exists, which is true in any source checkout; no step builds or inspects a wheel.
`pr_info/steps/step_2.md:154` — medium — Acceptance criterion "the new contract fails when `probe.py` imports a project module" is closed by a manual add-run-revert procedure that leaves no automated evidence, against the knowledge base's "no manual tests" rule.
`pr_info/steps/step_6.md:110` — low — Stale mapping row: `server.venv_path` at `pytest_tool.py:111` was already replaced by `bin_dir` in step 1, so this substitution no longer applies.
`pr_info/steps/step_2.md:113` — low — `info.importable.get(tool_name, False)` silently reports "unavailable" for any name absent from `PROBED_MODULES`; nothing ties that tuple to the actual `_is_tool_available` call sites (pylint, pytest, mypy, black, isort).
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_2.md, add tests/test_tool_availability.py to the Modified list and specify updating the 10 patch("mcp_tools_py.server.execute_command") sites outside TestIsToolAvailable (lines 34, 51, 80, 96, 378, 403, 428, 453, 487, 527) so removing the now-dead execute_command import from server.py leaves the suite green.', "In pr_info/steps/step_1.md:150, fix the prescribed patch target to match the step's own ALGORITHM at lines 60 and 66, which uses Path.exists(): patch pathlib.Path.exists (or change the algorithm to os.path.exists) so the rewritten availability tests actually exercise the code path described.", 'In pr_info/steps/step_1.md:156, replace the claim that TestResolvePythonExecutable may keep asserting on server._resolved_python: tests/test_tool_availability.py:43-44 and 60-61 compare against os.path.join("/my/venv", "Scripts", "python.exe"), which differs from str(Path(venv_path)/sub/exe) on Windows. Specify the expected string form and the required test updates.', 'In pr_info/steps/step_1.md, add tests/test_server_params.py to the Modified list and specify updating tests/test_server_params.py:83, which asserts check_code_with_pytest(..., venv_path=None, ...) — the venv_path→bin_dir rename plus the now non-None value breaks that assertion.', 'In pr_info/steps/step_6.md, add tests/test_server_params.py to the Modified list and specify updating the _check_tool_availability patches/assignments and _is_tool_available references at lines 52, 58, 104, 108, 141, 145, 411, 415, 500, 527, 746, 797, 799 so the rename and deletion land green.', 'In pr_info/steps/step_6.md:95, extend the test-update scope beyond TestToolHandlerShortCircuit: deleting _resolved_python also breaks TestResolvePythonExecutable (tests/test_tool_availability.py:44, 61, 90, 103) and tests/test_server_params.py:78. Name those cases and state how they are updated or removed.', 'In pr_info/steps/step_2.md:185, either drop the acceptance criterion "probe.py is present in a built wheel, not only in a source checkout" or replace the probe_script_path() existence assertion with a check that actually verifies wheel packaging (e.g. a test that builds the wheel and inspects its contents, or an assertion on the package-data/include configuration).', 'In pr_info/steps/step_2.md:154, replace the manual add-run-revert procedure for "the new contract fails when probe.py imports a project module" with an automated test that leaves evidence in CI, per the knowledge base\'s no-manual-tests rule.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-09-04
**Findings**:
I'll gather context from the knowledge base, the issue, and the plan files.`pr_info/steps/step_3.md:100` — high — README edit is scoped to the `get_library_source` table row at `README.md:440`, but `README.md:103-104` and the entire "Environment Configuration" section (`README.md:135-173`) still instruct users that `--python-executable`/`--venv-path` must point at "the tool's own venv, not the project's runtime venv", and label `--venv-path /path/to/your/project/.venv` as the "Incorrect Configuration" — which after this change is exactly the configuration the fix requires; no step corrects it, so the README contradicts both the new invariant and step 1's corrected `main.py` help text.

`pr_info/steps/step_6.md:109` — medium — the `_resolved_python` reader table omits `tests/test_tool_availability.py:520` (`test_resolved_python_passed_to_pytest_runner` asserts `call_kwargs.kwargs["python_executable"] == server._resolved_python`), so the enumerated migration for the removed attribute is incomplete.

`pr_info/steps/step_1.md:81` — medium — after step 1 nothing reads `ToolServer.venv_path` or `ToolServer.python_executable` (`pytest_tool.py` switches to `bin_dir`, `_resolve_python_executable` is replaced), yet no step deletes those write-only attributes, the matching `vulture_whitelist.py:26-27` entries, or the now-stale `venv_path`/`python_executable` docstrings at `server.py:64-65` and `server.py:282-283`.

`pr_info/steps/step_4.md:40` — medium — `interpreter: str | None = None` on `list_symbols`/`find_references` contradicts step 3's own rule at `pr_info/steps/step_3.md:33` ("`interpreter` is **required**, not defaulted. A default would silently reproduce the exact bug being fixed"); here the default is justified only by avoiding edits to thirteen tests, and it leaves the pre-fix `VIRTUAL_ENV` resolution path reachable from any future call site.

`pr_info/steps/step_7.md:62` — low — `docs/architecture/dependencies/readme.md` requires regenerating `dependency_graph.html` and `pydeps_graph.*` after `tach.toml` changes; steps 3 and 7 both edit `tach.toml` and neither regenerates the graphs.
**Decisions**:
Verdict(decision='tasks', tasks=['Extend the README work in pr_info/steps/step_3.md beyond the get_library_source table row at README.md:440: correct README.md:103-104 and the entire "Environment Configuration" section (README.md:135-173), which still tells users --python-executable/--venv-path must point at the tool\'s own venv rather than the project runtime venv and labels `--venv-path /path/to/your/project/.venv` as "Incorrect Configuration" — the configuration the fix now requires. Make it consistent with the new invariant and step 1\'s corrected main.py help text.', 'In pr_info/steps/step_6.md:109, add tests/test_tool_availability.py:520 (test_resolved_python_passed_to_pytest_runner, which asserts call_kwargs.kwargs["python_executable"] == server._resolved_python) to the _resolved_python reader table and specify how it is migrated.', 'Add an explicit cleanup to the plan (step 1 or step 6, wherever the attributes go dead) for the now write-only ToolServer.venv_path and ToolServer.python_executable: delete the attributes, remove the matching vulture_whitelist.py:26-27 entries, and update the stale docstrings at server.py:64-65 and server.py:282-283.', 'Resolve the contradiction between pr_info/steps/step_4.md:40 and pr_info/steps/step_3.md:33: make `interpreter` a required parameter on list_symbols/find_references rather than defaulting to None, and list the affected tests as work in step 4 instead of using the default to avoid editing them — the default keeps the pre-fix VIRTUAL_ENV resolution path reachable.'], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-09-04
**Findings**:
I'll gather context: knowledge base, the issue and its links, and the plan files.`pr_info/steps/step_4.md:70` — high — the `try` wraps only `jedi.Project(...)`, which never builds the environment: `Project.get_environment()` is lazy and first runs from `jedi.Script(...)` (`jedi_tools.py:27,99`), which sits outside every `try` in that file — so `InvalidPythonEnvironment` still escapes as a traceback and the failure is never cached, contradicting step_4.md:85 and step_4.md:94.
`pr_info/steps/step_1.md:100` — high — replacing the five annotated `self._<tool>_binary` assignments with `setattr(self, f"_{...}_binary", ...)` leaves the attributes undeclared, so mypy reports `attr-defined` at the nine readers (`ruff_check_tool.py:41,66`, `ruff_fix_tool.py:42,66`, `bandit_tool.py:42,66`, `lint_imports_tool.py:39,48`, `tach_tool.py:29,41`, `vulture_tool.py:40,64`) while step 1 still requires mypy to pass.
`pr_info/steps/step_1.md:204` — high — the `str(Path(...))` normalization table omits `tests/test_tool_availability.py:521`, where `test_resolved_python_passed_to_pytest_runner` asserts `python_executable == "/custom/python"`; on Windows that becomes `\custom\python`, so the test goes red in step 1 and step 6 only rewrites `:520`.
`pr_info/steps/step_6.md:73` — medium — folding the nine bespoke unavailable-tool strings into `unavailable_message` changes text asserted at `tests/test_tool_availability.py:396,397,421,422,446,447,547` ("pytest/pylint/mypy is not available", "Restart the server"); only `:542` is named as needing care.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_4.md:70, widen the error handling: jedi.Project.get_environment() is lazy and first runs inside jedi.Script(...) at jedi_tools.py:27 and :99, which sit outside the try. Move or extend the try so InvalidPythonEnvironment raised at Script construction is caught and cached, matching the behaviour promised at step_4.md:85 and :94.', 'In pr_info/steps/step_1.md:100, keep the five _<tool>_binary attributes declared (annotated class/instance attributes) instead of assigning them only via setattr(self, f"_{...}_binary", ...); otherwise mypy reports attr-defined at ruff_check_tool.py:41,66, ruff_fix_tool.py:42,66, bandit_tool.py:42,66, lint_imports_tool.py:39,48, tach_tool.py:29,41 and vulture_tool.py:40,64 while step 1 still requires mypy to pass.', 'In pr_info/steps/step_1.md:204, add tests/test_tool_availability.py:521 to the str(Path(...)) normalization table: test_resolved_python_passed_to_pytest_runner asserts python_executable == "/custom/python", which normalizes to \\\\custom\\\\python on Windows and goes red in step 1 — step 6 only rewrites :520.'], escalate_reason=None)
**Changes**:
applied

## Round 4 — 2026-09-04
**Findings**:
I'll gather context from the knowledge base, the issue tree, and the plan files.`pr_info/steps/step_6.md:172` — high — Step 6 deletes `_tool_availability`, `_is_tool_available` and the five `_<tool>_binary` attributes, but names only `TestToolHandlerShortCircuit` and `TestResolvePythonExecutable` in `tests/test_tool_availability.py`; the whole of `TestCheckToolAvailability` (`:111-236`, seven tests asserting `server._tool_availability == {...}`, `server._lint_imports_binary`, `_vulture_binary`, `_tach_binary`) and `TestIsToolAvailable` (`:244-347`, five tests calling `server._is_tool_available(...)` and reading `server._tool_availability[...]`) have no prescribed migration and go red — the step cannot land green as written.
`pr_info/steps/step_6.md:73` — medium — Folding the nine bespoke unavailable-tool strings into `ToolContext.unavailable_message` must preserve the substrings `"<tool> is not available"` and `"Restart the server"`, asserted at `tests/test_tool_availability.py:396,397,421,422,446,447,547`, `tests/test_checker_tools.py:194,585,629,673`, `tests/test_formatter_tools.py:301` and `tests/test_code_checker_bandit/test_integration.py:45,46`; the step does not say so, and the wording it points at (`step_2.md:130` "<tool> is not installed in <interpreter> (Python <version>)") contains neither.
`pr_info/steps/step_2.md:226` — medium — The `get_environment_info.cache_clear()` autouse fixture is scoped to `tests/test_environment_info.py` only, yet the `lru_cache` is process-wide; step 6's `tests/test_tool_context.py` and any server test that reaches a real probe for `sys.executable` can then observe a cached result from another module under `-n auto`.
`pr_info/steps/step_1.md:239` — low — "Three further keyword call sites in the same file" is wrong: `tests/test_code_checker/test_runners.py` references `venv_path` only at `:229`; no keyword call site passes it.
`pr_info/steps/summary.md:150` — low — The "Files modified" table omits `tests/conftest.py` (shared `ToolContext` fixture added in step 6, `step_6.md:187`) and `tests/test_utility_tools.py` (step 7, `step_7.md:117`).
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_6.md:172, extend the test migration scope in tests/test_tool_availability.py beyond TestToolHandlerShortCircuit and TestResolvePythonExecutable: the whole of TestCheckToolAvailability (:111-236, seven tests asserting server._tool_availability == {...}, server._lint_imports_binary, _vulture_binary, _tach_binary) and TestIsToolAvailable (:244-347, five tests calling server._is_tool_available(...) and reading server._tool_availability[...]) depend on the attributes the step deletes. Prescribe how each class is rewritten or removed so step 6 lands green.'], escalate_reason=None)
**Changes**:
applied

## Round 5 — 2026-09-04
**Findings**:
I'll gather context: knowledge base, the issue chain, and the plan files.`pr_info/steps/step_6.md:73` — high — Folding the nine bespoke unavailable-tool strings into `ToolContext.unavailable_message` drops the substrings `"<tool> is not available"` and `"Restart the server"` that 13 assertions depend on (`tests/test_tool_availability.py:396,397,421,422,446,447,547`, `tests/test_checker_tools.py:194,585,629,673`, `tests/test_formatter_tools.py:301`, `tests/test_code_checker_bandit/test_integration.py:45,46`); the wording the step points at (`pr_info/steps/step_2.md:130`, "<tool> is not installed in <interpreter> (Python <version>)") contains neither, and three of those four test files are not listed as needing message-text updates, so step 6 cannot land green as written.

`pr_info/steps/step_6.md:177` — medium — "keep that assertion against `unavailable_message`" is unachievable as stated: `tests/test_tool_availability.py:542,546` sets `_lint_imports_binary = "/mock/venv/bin/lint-imports"` and asserts that full path appears, but after step 6 the attribute is gone and the message names `environment.bin_dir` of a server built without `--venv-path` (`Path(sys.executable).parent`); the step must prescribe constructing the context over a controlled `bin_dir` and asserting on `bin_dir/<tool>`.

`pr_info/steps/step_2.md:226` — medium — The `get_environment_info.cache_clear()` autouse fixture is scoped to `tests/test_environment_info.py`, but the `lru_cache` is process-wide and step 6 adds a second consumer (`tests/test_tool_context.py`) plus server tests that can reach a real probe for `sys.executable`; the fixture belongs in `tests/conftest.py`.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_6.md:73, specify that the consolidated ToolContext.unavailable_message must preserve the substrings "<tool> is not available" and "Restart the server", or else list every assertion to update: tests/test_tool_availability.py:396,397,421,422,446,447,547, tests/test_checker_tools.py:194,585,629,673, tests/test_formatter_tools.py:301 and tests/test_code_checker_bandit/test_integration.py:45,46. The wording referenced at pr_info/steps/step_2.md:130 ("<tool> is not installed in <interpreter> (Python <version>)") contains neither substring, and three of those four test files are not currently in the step\'s Modified list — add them.'], escalate_reason=None)
**Changes**:
applied
