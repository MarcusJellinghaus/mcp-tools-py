# review-plan review log 1

## Round 1 — 2026-09-01
**Findings**:
I'll gather context first — knowledge base, the issue, and the plan files.`pr_info/steps/step_12.md:15` — medium — README's per-tool *Pytest Parameters* and *Mypy Parameters* tables (README.md:62-66, 74-80) enumerate every MCP tool argument, but step 12 adds only the CLI row; both tables go stale once steps 8 and 10 add `timeout_seconds`
`pr_info/steps/step_9.md:62` — medium — `get_pylint_prompt` already prefixes `"Pylint analysis failed: "` (reporting.py:235), so the planned error text renders as "Pylint analysis failed: Pylint execution timed out after N seconds"; step 9 never notices the double prefix that step 8 documents for mypy
`pr_info/steps/step_8.md:78` — medium — accepting "Mypy execution failed: Mypy execution timed out after 600 seconds" is a needless regression in readability of the exact message the issue was filed about; dropping the "Mypy execution" prefix from the runner error is free and in scope
`pr_info/steps/step_8.md:54` — medium — the same new `timeout_seconds` argument fails differently on the only two tools that accept it: mypy re-raises as an MCP protocol error, pytest returns text (step_10.md:44); the issue names `timeout_seconds=0` on `run_mypy_check` as the likeliest validation failure
`pr_info/steps/step_3.md:33` — medium — tach's false green also occurs when `execute_command` sets `execution_error` without `timed_out` (FileNotFoundError/PermissionError/OSError path); the plan reorders that exact branch but leaves the second false-pass route unfixed
`pr_info/steps/step_4.md:36` — medium — same as tach: vulture still returns "vulture produced no output." when `execution_error` is set without `timed_out`, in the function the step already edits
`pr_info/steps/step_1.md:21` — low — `_CONFIG_SECTION` and `_SHARED_TIMEOUT_KEY` are declared in WHAT but the ALGORITHM at step_1.md:79-81 hardcodes the same literals, so the step ships two unused module constants
`pr_info/steps/step_12.md:60` — low — "fails every tool call" overstates the consequence; the refactoring, utility and inspect_library tools read no project config and stay unaffected
`pr_info/steps/summary.md:30` — low — no follow-up is recorded for the mcp-config `ParameterDef` the issue names as a dependency, so `--check-timeout` remains unreachable for mcp-config-managed installs
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_3.md, fix the tach false-green fully: the result must not report success when `execute_command` sets `execution_error` without `timed_out` (FileNotFoundError/PermissionError/OSError path) — handle both branches, not just the timeout reorder.', 'In pr_info/steps/step_4.md, apply the same fix for vulture: do not return "vulture produced no output." when `execution_error` is set without `timed_out`; surface the execution error in the same function the step already edits.', 'In pr_info/steps/step_8.md, drop the redundant "Mypy execution" prefix from the runner timeout error so the final message reads "Mypy execution failed: timed out after N seconds" rather than double-prefixing.', 'In pr_info/steps/step_9.md, account for `get_pylint_prompt` already prefixing "Pylint analysis failed: " (reporting.py:235) — specify the raw timeout text without a second "Pylint" prefix.', 'In pr_info/steps/step_8.md and step_10.md, make `timeout_seconds` validation failures behave the same way on both tools (mypy currently re-raises as an MCP protocol error while pytest returns text); pick one behaviour and state it in both steps, covering `timeout_seconds=0` on `run_mypy_check`.', 'In pr_info/steps/step_12.md, extend the README documentation task to update the per-tool Pytest Parameters and Mypy Parameters tables (README.md:62-66, 74-80) with the new `timeout_seconds` argument, not just the CLI row.', 'In pr_info/steps/step_1.md, resolve the `_CONFIG_SECTION` / `_SHARED_TIMEOUT_KEY` inconsistency: either use the constants in the ALGORITHM at lines 79-81 instead of the hardcoded literals, or drop the constants from WHAT.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-09-01
**Findings**:
I'll gather context: knowledge base, the issue tree, and the plan files.`pr_info/steps/summary.md:66` — high — "Nine of the ten registrars already wrap their work in a `try/except` returning an error message" is false: `checker_tools/pylint_tool.py:83-92` re-raises exactly like `mypy_tool`, so only eight do; the "no `int | str` companion wrapper" rationale rests on a wrong count
`pr_info/steps/step_9.md:46` — high — resolving `server.resolve_timeout("pylint")` inside pylint_tool's existing `try` puts it under an `except Exception: raise`, so an invalid `pylint-timeout` or malformed `pyproject.toml` surfaces as an MCP protocol error while every other checker returns text; step 8 added an explicit `except ValueError` for mypy for precisely this reason and step 9 does not
`pr_info/steps/step_5.md:44` — medium — lint-imports still reports `"ERROR: lint-imports output could not be parsed"` when `execute_command` sets `execution_error` without `timed_out` (`return_code=1`, empty stdout/stderr → `summary is None` → state ERROR); steps 3 and 4 fix that sibling branch, step 5 leaves the wrong cause in the same function it opens
`pr_info/steps/step_11.md:66` — low — only the timeout half of black/isort's "success=False with empty output, no reason given" is fixed; the `execution_error`-without-`timed_out` path still returns an empty `output` with no reason in the function the step already edits
`pr_info/steps/step_10.md:41` — low — an invalid `timeout_seconds` on `run_pytest_check` reaches the generic handler and returns `"Unexpected error running pytest: ValueError: ..."`, labelling a caller input error as unexpected, while step 8 has mypy return `"Error: <msg>"`
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/summary.md:66, correct the claim that nine of ten registrars wrap their work in try/except returning an error message — `checker_tools/pylint_tool.py:83-92` re-raises like `mypy_tool`, so the count is eight; restate the rationale for having no `int | str` companion wrapper on the corrected facts (or replace it if it no longer holds).', 'In pr_info/steps/step_9.md:46, move or guard the `server.resolve_timeout("pylint")` call so an invalid `pylint-timeout` or malformed `pyproject.toml` returns text like the other checkers instead of escaping the existing `except Exception: raise` as an MCP protocol error — mirror the explicit `except ValueError` step 8 added for mypy.', 'In pr_info/steps/step_5.md:44, fix the sibling branch already handled in steps 3 and 4: lint-imports must not report "ERROR: lint-imports output could not be parsed" when `execute_command` sets `execution_error` without `timed_out`; surface the actual execution error instead.', 'In pr_info/steps/step_11.md:66, extend the black/isort fix beyond the timeout case: the `execution_error`-without-`timed_out` path must also report a reason rather than returning `success=False` with empty output, in the function the step already edits.'], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-09-01
**Findings**:
I'll gather context from the knowledge base, the issue tree, and the plan files.`pr_info/steps/step_12.md:8` — medium — retitling `docs/pyproject-configuration.md` leaves `docs/README.md:18` indexing it as "**Pylint Configuration** — How pylint reads `pyproject.toml`…"; step 12 lists no update to that index entry, so the docs index will misdescribe the file that now owns `[tool.mcp-tools-py]`
`pr_info/steps/step_10.md:41` — medium — step 10 requires that "the two tools sharing this argument must not report a bad value differently", but routing pytest's `ValueError` through the existing generic handler yields `Unexpected error running pytest: ValueError: …` while step 8 has mypy return `Error: …`; the stated parity is not achieved by the specified implementation
`pr_info/steps/summary.md:158` — medium — "the two existing tests" undercounts: `tests/test_code_checker/test_runners.py:384-393` is a third existing timeout test built on the banned `timed_out=True` + `execution_error=None` state, and step 10 corrects only the retry test at ~586, so a fictional-state pytest timeout case survives the branch reorder unchanged
**Decisions**:
Verdict(decision='dismiss', tasks=[], escalate_reason=None)
**Changes**:
dismiss
