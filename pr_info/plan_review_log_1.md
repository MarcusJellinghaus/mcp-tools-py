# review-plan review log 1

## Round 1 — 2026-09-02
**Findings**:
I'll gather context: knowledge base, the issue tree, and the plan files.`pr_info/steps/step_3.md:45` — high — Doc content item 6 instructs writing that none of the flags the server still passes are in `OPTIONS_AFFECTING_CACHE`; probed on the installed mypy (50 entries): `follow_imports: True`, `disable_error_code: True`. Shipping that sentence documents caller-supplied `follow_imports` / `disable_error_codes` as cache-safe when they silently split the cache — the exact trap this issue closes.
`pr_info/steps/step_1.md:127` — high — "Every flag left here is outside `OPTIONS_AFFECTING_CACHE`" is false for the two conditionals emitted at `:121-122`; only the unconditional flags have an empty intersection.
`pr_info/steps/summary.md:29` — medium — Same overclaim ("the flags the server still passes are exactly the ones outside `OPTIONS_AFFECTING_CACHE`"); contradicted by its own per-call row at `:36`.
`pr_info/steps/step_1.md:69` — medium — `mypy_tool.py:48-53` documents `follow_imports` as "'normal' (default)"; after the change no flag is sent and the project's config decides. Plan removes only the `strict` docstring block, leaving a second client-visible docstring that misstates the flag set.
`pr_info/steps/step_1.md:101` — medium — `README.md:80` row `follow_imports | string | 'normal'` becomes wrong for the same reason; plan lists only the `strict` row (`:77`) and `:427`.
`pr_info/steps/step_1.md:15` — medium — `tests/mcp_tools_py_manual/TEST_PLAN.md:185-197` documents test 3a "Strict mode (default)" and 3b `run_mypy_check(strict=False, ...)`; a documented call of the removed parameter, absent from every plan file list.
`pr_info/steps/step_2.md:13` — medium — `tests/test_error_transparency.py:220-239` (`TestMypyTimeout`) already tests this branch and is not in the WHERE table; the planned `test_runners.py` tests overlap it, and its `"Process timed out after" not in result.error` assertion constrains the new message.
`pr_info/steps/step_1.md:142` — medium — Plan invents a local `_capture` fake building `CommandResult(...)` directly; `tests/conftest.py:make_command_result` plus the `@patch("mcp_tools_py.code_checker_mypy.runners.execute_command")` pattern already exist and are used by the mypy tests.
`pr_info/steps/summary.md:111` — medium — Measurement 1 is deferred with no owner, trigger or acceptance criterion, against the issue's decision that measurements run as step zero and its statement that measurement 1 "can close most of this issue outright".
`pr_info/steps/step_1.md:23` — low — "keeping the existing keys untouched" leaves `pyproject.toml:109` (`# disallow_untyped_decorators = false # can create issues with pytest fixtures`) actively contradicting the new `strict = true`, which enables it.
`pr_info/steps/step_1.md:81` — low — "The two internal docstrings only lose their `strict:` line" is incomplete: `runners.py:58` also documents the removed `config_file`, and `:55` describes `follow_imports` as always applied.
`pr_info/steps/step_1.md:90` — low — `tools/checks2clipboard.bat:142` ("Run mypy with strict checks") is a sixth stale reference; the plan lists only `:144`.
`pr_info/steps/step_1.md:170` — low — The "no config at all" temp project can still pick up a user-level mypy config (`~/.mypy.ini`, XDG), making `return_code == 0` environment-dependent; the test needs isolation from user config.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_3.md:45, fix doc content item 6: `follow_imports` and `disable_error_code` ARE in mypy's OPTIONS_AFFECTING_CACHE, so do not document the remaining server-passed flags as cache-safe. Instead state that the unconditional flag set is cache-neutral, and warn explicitly that caller-supplied `follow_imports` / `disable_error_codes` split the mypy cache.", "In pr_info/steps/step_1.md:127, correct the claim 'Every flag left here is outside OPTIONS_AFFECTING_CACHE' — it holds only for the unconditional flags, not the two conditionals emitted at :121-122. Scope the sentence accordingly.", 'In pr_info/steps/summary.md:29, fix the same overclaim so it matches the per-call row at :36: only the unconditional flags are outside OPTIONS_AFFECTING_CACHE.', 'Add to the step_1 docstring cleanup list: mypy_tool.py:48-53, which documents `follow_imports` as "\'normal\' (default)". After the change no flag is sent by default and the project\'s config decides — update that client-visible docstring too.', "Add README.md:80 (`follow_imports | string | 'normal'`) to the step_1 documentation-update list alongside the `strict` rows at :77 and :427.", "Add tests/mcp_tools_py_manual/TEST_PLAN.md:185-197 to the step_1 update list — tests 3a 'Strict mode (default)' and 3b `run_mypy_check(strict=False, ...)` reference the removed parameter.", 'In step_2, add tests/test_error_transparency.py:220-239 (`TestMypyTimeout`) to the WHERE table: it already covers the timeout branch, its `"Process timed out after" not in result.error` assertion constrains the new message, and the planned test_runners.py tests must not duplicate it.', 'In step_1.md:142, drop the invented local `_capture` fake; use the existing `tests/conftest.py:make_command_result` helper with the established `@patch("mcp_tools_py.code_checker_mypy.runners.execute_command")` pattern used by the current mypy tests.', "In summary.md:111, either schedule Measurement 1 as step zero per the issue's decision (it may close most of the issue outright), or, if it stays deferred, give it an explicit owner, trigger, and acceptance criterion."], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-09-02
**Findings**:
I'll gather context: knowledge base, the issue chain, and the plan files.`pr_info/steps/summary.md:110` — high — Out-of-scope rationale is factually wrong: fixing the client-visible "Mypy found type issues that need attention:" prefix on a timeout needs no change to `get_mypy_prompt`'s return contract — `_format_mypy_result` (`checker_tools/__init__.py:130`) can branch on the `"Mypy execution failed:"` prefix `get_mypy_prompt` already emits, so the plan ships an honest message wrapped in a false headline.
`pr_info/steps/step_1.md:204` — medium — "run_mypy_check must pass through the new code path — that is the migration's own proof" is unachievable in-session: `.mcp.json` starts the server from `${MCP_CODER_VENV_PATH}\mcp-tools-py.exe` before the edit, so the running process keeps the old modules; only pytest and CI (fresh processes) exercise the new path.
`pr_info/steps/step_1.md:167` — medium — Planned assertion `"MYPYPATH" not in env` tests the ambient environment rather than the code: an inherited `MYPYPATH` survives `os.environ.copy()` and fails the test for an unrelated reason; no `monkeypatch.delenv` is planned, unlike the `MYPY_NUM_WORKERS` case at `:168`.
`pr_info/steps/step_2.md:39` — low — Line reference for the timeout branch is stale: it is `runners.py:152-158`, not `:146-152`.
`pr_info/steps/step_1.md:56` — low — Same drift in the env block: the `MYPYPATH` assignment is `runners.py:139` and `env = os.environ.copy()` is `:138`, not `:137`/`:136`.
`pr_info/steps/step_1.md:112` — low — `tests/mcp_tools_py_manual/PROGRESS_TRACKER.md:40` (`| 3b | Non-strict |`) is the companion row to the TEST_PLAN 3a/3b rewrite and appears in no plan file list.
`pr_info/steps/step_3.md:47` — low — `--cache-dir` is listed among the flags passed on **every** call; it is emitted only when the caller supplies `cache_dir` (step_1.md:132).
`pr_info/steps/summary.md:75` — low — Issue #107's two open checkboxes (add `strict = true` / `warn_unreachable = true`, verify mypy passes) are completed by step 1, but no plan file mentions ticking or closing them.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/summary.md:110, drop the out-of-scope rationale for the timeout headline: `_format_mypy_result` (checker_tools/__init__.py:130) can branch on the `"Mypy execution failed:"` prefix `get_mypy_prompt` already emits, so no change to `get_mypy_prompt`\'s return contract is needed. Bring the fix in scope so a timeout is not reported under the "Mypy found type issues that need attention:" headline.', 'In pr_info/steps/step_1.md:204, remove the claim that `run_mypy_check` passing through the new code path is in-session proof — `.mcp.json` launches the server from the pre-edit executable, so the running process keeps the old modules. State that pytest and CI (fresh processes) are the verification.', 'In pr_info/steps/step_1.md:167, make the `"MYPYPATH" not in env` assertion test the code rather than the ambient environment: add a `monkeypatch.delenv("MYPYPATH", raising=False)` mirroring the `MYPY_NUM_WORKERS` handling at :168, since an inherited value survives `os.environ.copy()`.'], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-09-02
**Findings**:
I'll gather context first — knowledge base, issue #225 and links, and the plan files.`pr_info/steps/step_3.md:56` — high — Doc item 6 instructs writing that `--namespace-packages` is one of the no-op flags the server still passes and is "kept only as tidying", but step 1 (`step_1.md:53`, ALGORITHM `:130-135`) removes it. Following this produces a user-facing flag-set table that lists a flag the server no longer sends — in the one section whose purpose is to state the flag set precisely.

`pr_info/steps/step_2.md:46` — high — The timeout message derives the cache path as `os.path.join(project_dir, cache_dir or ".mypy_cache")`, ignoring `[tool.mypy] cache_dir`. Under the config-ownership regime this step ships with, a project that sets `cache_dir` in config gets a message reporting size/mtime for a path mypy never wrote — a false fact in the message step 2 exists to make honest, and the input to measurement 2 (`summary.md:135`).

`pr_info/steps/step_3.md:47` — medium — `--cache-dir` is listed among the flags passed on **every** call; `step_1.md:132` emits it only when the caller supplies `cache_dir`. (Raised in round 2, not applied.)

`pr_info/steps/step_1.md:183` — medium — The "no config at all" temp project is not isolated from user-level mypy config (`~/.mypy.ini`, `$XDG_CONFIG_HOME/mypy/config`), so `return_code == 0` is environment-dependent — in the test that serves as the migration's behaviour-neutrality proof. (Raised in round 1, not applied.)
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_3.md:56, remove `--namespace-packages` from doc item 6's list of no-op flags the server still passes: step 1 (step_1.md:53, ALGORITHM :130-135) removes it, so the documented flag-set table must not list it.", 'In pr_info/steps/step_2.md:46, stop deriving the cache path as `os.path.join(project_dir, cache_dir or ".mypy_cache")` — that ignores `[tool.mypy] cache_dir`, which the config-ownership regime now lets the project set. Either resolve the effective cache dir from the project\'s mypy config before reporting size/mtime, or omit the cache-path detail when the caller supplied no `cache_dir` rather than reporting a path mypy may never have written.'], escalate_reason=None)
**Changes**:
applied

## Round 4 — 2026-09-02
**Findings**:
I'll gather context first — knowledge base, the issue, and the plan files.`pr_info/steps/step_3.md:47` — medium — `--cache-dir` is listed among the flags passed on **every** call; `step_1.md:132` emits it only when the caller supplies `cache_dir`, so the documented flag-set table would be wrong in the one section whose purpose is stating the flag set precisely (raised rounds 2 and 3, not applied)
`pr_info/steps/step_1.md:183` — medium — the "no config at all" temp project is not isolated from user-level mypy config (`~/.mypy.ini`, `$XDG_CONFIG_HOME/mypy/config`, `~/.config/mypy/config`), making `return_code == 0` environment-dependent in the test that serves as the migration's behaviour-neutrality proof (raised rounds 1 and 3, not applied)
`pr_info/steps/step_1.md:107` — medium — `mypy_tool.py:53-55` ("Defaults to .mypy_cache in the project directory") and `README.md:81` (`cache_dir | string | None (.mypy_cache)`) both become inaccurate once `[tool.mypy] cache_dir` owns the setting — the exact fact step 2's `_resolve_cache_dir` is built around — yet neither client-visible text appears in any plan file's update list
`pr_info/steps/step_1.md:96` — low — `tools/checks2clipboard.bat:142` ("REM Run mypy with strict checks") is a sixth stale site listed nowhere, and `step_3.md:95`'s verification regex (`--strict|strict mode|mypy \(strict\)`) does not match "strict checks", so it survives both steps
`pr_info/steps/step_3.md:22` — low — placing the mypy section directly after `## Finer-grained code control` inserts it between two pylint-only sections (`docs/pyproject-configuration.md:105` and `:129`), which is the interleaving the issue asked to avoid
**Decisions**:
Verdict(decision='dismiss', tasks=[], escalate_reason=None)
**Changes**:
dismiss
